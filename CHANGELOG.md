# มีอะไรใหม่

อัปเดต: `cd sararif-cc && git pull`
เช็คว่าตัวเองเวอร์ชันอะไร: `bun doctor.ts`

---

## 1.1.0 — 17 กันยายน 2026

### 🐛 แก้บั๊กใหญ่: ซับตัดกลางคำ

ตัวนับความยาวบรรทัด **นับสระบน/สระล่าง/วรรณยุกต์/การันต์เป็นความกว้างด้วย**
ทั้งที่มันซ้อนอยู่บนพยัญชนะ ไม่กินที่เลย โปรแกรมจึงคิดว่าบรรทัดเต็มเร็วกว่าความจริง
แล้วไปตัดกลางคำ

| คำ | โปรแกรมคิดว่ากว้าง | กว้างจริง | ผลที่เคยได้ |
|---|---|---|---|
| `เฟรนช์ฟรายส์` | 12 | **10** | ถูกผ่าเป็น `เฟรนช์ฟราย` / `ส์ก็มี` |
| `อ่างล้างหน้า` | 12 | **9** | ถูกผ่ากลาง |
| `เครื่องทำน้ำอุ่น` | 16 | **11** | พอดีบรรทัด แต่ถูกซอยเป็นพยางค์ |

**⚠️ ผลข้างเคียงที่ต้องรู้:** `maxchars` ตอนนี้หมายถึง *ความกว้างที่ตาเห็น* ไม่ใช่จำนวนตัวอักษร
บรรทัดจะยาวขึ้นกว่าเดิมเล็กน้อยที่ค่าเดิม ถ้าชอบแบบเก่าให้ลด `maxchars` ลง 1-2
(`bun setup.ts` หรือ `--maxchars`)

### 🐛 คำยืมถูกซอยแหลกจนซับตัดกลางคำ

ตัวตัดคำไม่รู้จักคำยืม เลยซอย `เฟรนช์ฟรายส์` → `[เฟรนช์·ฟ·ราย·ส์]` ·
`เทริยากิ` → `[เท·ริ·ยา·กิ]` แล้วจุดตัดบรรทัดไปตกกลางคำได้

- เติมคำอาหาร/คาเฟ่ที่เจอบ่อยเข้าคลังคำ (ชาบู · ซูชิ · ติ่มซำ · ครัวซองต์ · บาริสต้า · บิงซู ฯลฯ)
- เพิ่ม **ตาข่ายกันคำแตก** — เศษที่ไม่ใช่คำจริงและสั้นกว่า 3 ตัว จะถูกเชื่อมกลับเข้าคำข้างหน้าเอง
  แม้เป็นคำที่ยังไม่มีในคลัง
- ชื่อร้าน/ชื่อคนของคุณเอง ใส่ที่ `~/.sararif-cc/words.txt` ได้เหมือนเดิม (1 บรรทัด 1 คำ)

### ✏️ กฎการตัดบรรทัด: `"แล้ว"` ไม่ใช่คำเชื่อมอีกต่อไป

เดิมห้าม `แล้ว` ทั้งจบบรรทัดและขึ้นต้นบรรทัด แต่จากการเทียบกับซับที่คนตัดเอง
เขาใช้ทั้ง 2 ทาง — บรรทัดที่จบด้วย `ของมาแล้ว` และบรรทัดที่ขึ้นต้นด้วย `แล้วโรยพริกมาเต็ม`
เพราะมันเป็นคำ *จบประโยค* และ *เปิดประโยค* ไม่ใช่คำเชื่อมที่ต้องเกาะ

### 🔔 บอกเมื่อมีเวอร์ชันใหม่

รันคำสั่งไหนก็ได้ ถ้ามีของใหม่จะขึ้นบรรทัดเตือนท้ายผลลัพธ์

- เช็ควันละครั้ง จำผลไว้ที่ `~/.sararif-cc/.update-check`
- เน็ตล่ม/ช้า = เงียบ ไม่ขึ้นอะไร ไม่ทำให้คำสั่งหลักพัง (หมดเวลาใน 2 วินาที)
- ไม่อยากให้เตือน: `SARARIF_CC_NO_UPDATE_CHECK=1` หรือใส่ `--no-update-check`

---

## 1.0.0

รุ่นแรก — ตัดช่วงเงียบ · ใส่ซับจากคำบรรยายอัตโนมัติของ CapCut · ใส่ hook ·
3 สไตล์ซับ (text / karaoke / genz) · รองรับ Windows (เบต้า)
