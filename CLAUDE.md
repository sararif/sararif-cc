# คู่มือสำหรับ AI (Claude) — คุณคือคนติดตั้งและคนตัดต่อแทนเจ้าของเครื่อง

ไฟล์นี้เขียนถึง **AI ที่กำลังช่วยเจ้าของเครื่องใช้ sararif-cc** (เครื่องมือใส่ซับไทย + ตัดช่วงเงียบ + ใส่ hook ลงโปรเจกต์ CapCut)

**สมมติฐานตั้งต้น: เจ้าของเครื่องเขียนโค้ดไม่เป็น** เขาอาจไม่เคยเปิด Terminal มาก่อน
งานของคุณคือทำให้เขา *ไม่ต้อง* เป็น — คุณรันคำสั่งแทน อธิบายเป็นภาษาไทยบ้านๆ และบอกชัดว่าขั้นไหนเขาต้องทำเองใน CapCut

---

## เป้าหมายสุดท้าย (นิยามคำว่า "ติดตั้งเสร็จ")

1. `bun doctor.ts` ขึ้น `✅ พร้อมใช้งาน`
2. เจ้าของเครื่องเข้าใจลูปการทำงาน: **ทำใน CapCut → ปิด CapCut → สั่งคุณ → เปิด CapCut ดูผล**
3. เขาได้ลองตัดคลิปจริงของเขา 1 คลิปจนเห็นซับใน CapCut แล้ว

## ขั้นติดตั้ง

ทำตามลำดับ อย่าข้าม:

1. ถ้ายังไม่มีโฟลเดอร์นี้ในเครื่อง: `git clone https://github.com/sararif/sararif-cc.git` (ไม่มี git ให้โหลด ZIP ตาม README)
2. ถ้าไม่มี `bun`: ติดตั้งก่อน `curl -fsSL https://bun.sh/install | bash` แล้วเปิด shell ใหม่/`source` ให้ PATH เห็น
3. รัน **`bun doctor.ts`** — นี่คือแหล่งความจริงเรื่อง "ขาดอะไร" ห้ามเดาเอง
4. ไล่แก้ทีละข้อที่ doctor บอก ❌ — ทุกข้อมีคำสั่งแก้แนบมาแล้ว รันให้เขาได้เลย ยกเว้นของที่มนุษย์ต้องทำเอง (ข้างล่าง)
5. รัน `bun doctor.ts` ซ้ำจนขึ้น ✅ แล้วรัน `bun setup.ts` หนึ่งครั้ง (สร้างไฟล์ตั้งค่า + โชว์ 3 โมเดล)

**ของที่คุณติดตั้งให้ไม่ได้ — ต้องบอกให้เจ้าของเครื่องทำเอง:**

| ของ | บอกเขาว่า |
|---|---|
| CapCut เวอร์ชันคอม | "โหลดฟรีจาก capcut.com ติดตั้งแล้วเปิดโปรแกรม สร้างโปรเจกต์ใหม่ 1 อัน (กด New project) แล้วปิดได้เลย" — ต้องเปิดครั้งแรกก่อน โฟลเดอร์โปรเจกต์ถึงจะถูกสร้าง |
| กด Auto captions | ทำในโปรแกรม CapCut เท่านั้น: เลือกคลิปบนไทม์ไลน์ → **Text → Auto captions → ภาษาไทย** → รอถอดเสร็จ → **ปิด CapCut ให้สนิท (Cmd+Q)** |
| ฟอนต์ "มหานคร" (ไม่บังคับ) | เปิด CapCut → ใส่ข้อความ → เลือกฟอนต์มหานคร 1 ครั้ง CapCut จะโหลดเก็บไว้ให้ |

**กฎเรื่องเงิน: ทางฟรีเป็นค่าเริ่มต้นเสมอ** ห้ามพาไปสมัคร/ใส่คีย์อะไรทั้งนั้นในการใช้ครั้งแรก
ทางฟรี = ใช้ซับที่ CapCut ถอดให้ (ไม่ต้องติดตั้งอะไรเพิ่ม ไม่ต้องมีคีย์) — เสนอ ElevenLabs Scribe (~฿1/คลิป 3 นาที) เฉพาะเมื่อเขาต้องการเวลาแม่นระดับคำจริงๆ และบอกราคาก่อนทุกครั้ง

## ลูปการใช้งาน (หลังติดตั้งเสร็จ)

เมื่อเจ้าของเครื่องบอกว่า "ตัดคลิปให้หน่อย" หรือบอกชื่อโปรเจกต์:

1. ถามให้ได้ 2 อย่าง: **ชื่อโปรเจกต์ใน CapCut** และ **คลิปแนวไหน** (เพื่อเลือกโมเดลจากตารางข้างล่าง)
2. ยืนยันว่าเขากด Auto captions ภาษาไทยแล้ว และ**ปิด CapCut แล้ว** (สคริปต์ปิดให้เองได้ แต่บอกเขาก่อนจะสุภาพกว่า)
3. รัน `bun check.ts <โปรเจกต์>` — อ่านอย่างเดียว ปลอดภัยเสมอ ได้ภาพรวมว่าคลิปมีปัญหาอะไร
4. รันแบบดูก่อน: `bun go.ts <โปรเจกต์> --style <โมเดล> --dry` แล้วสรุปให้เขาฟังว่าจะตัดอะไรออกบ้าง
5. เขาโอเคแล้ว → รันจริง (เอา `--dry` ออก) พร้อม `--highlight "คำเด็ด,ที่อยากเน้น"` และ hook ถ้ามี:
   `bun go.ts <โปรเจกต์> --style <โมเดล> --highlight "..." --l1 "บรรทัดบน" --l2 "เน้น [คำนี้]"`
6. บอกให้เขาเปิด CapCut ดูผล — ซับไม่ขึ้น = ปิด CapCut ให้สนิทแล้วเปิดใหม่ (มันแคชไฟล์ไว้)

**ข้อความ hook:** ให้เจ้าของเครื่องเป็นคนเคาะเสมอ — เสนอร่างจากเนื้อหาที่ถอดได้ (ดูจากผล `--dry`) ได้ 2-3 แบบ แต่อย่าใส่เองโดยไม่ถาม คำใน `[วงเล็บเหลี่ยม]` จะกลายเป็นสีเน้น

## 3 โมเดลสำเร็จรูป — เลือกตามแนวคลิป

| โมเดล | แนวคลิป | จอ | คลิปเป้า | หน้าตา |
|---|---|---|---|---|
| `--style text` | เล่าเรื่อง / diary / ขายของแบบเล่า | 9:16 แนวตั้ง | 25-30 วิ | ซับวลีสั้นขาว · hook 2-3 บรรทัดบนจอ |
| `--style karaoke` | สอน / บรรยาย / รีวิวแบบยาว | 16:9 แนวนอน | 45-90 วิ | ซับขึ้นทีละคำตามจังหวะพูด · hook เล็กมุมบน |
| `--style genz` | ไวรัล / ทริป / ไลฟ์สไตล์ | 9:16 แนวตั้ง | 15-25 วิ | ซับใหญ่เต็มจอ วลีมีคำเด็ดเหลืองทั้งวลี · hook ใหญ่กลางจอ |

ไม่แน่ใจ → ใช้ `text` · โปรเจกต์จอไม่ตรงกับโมเดล ระบบเตือนเองแต่ไม่ block · ทุกค่าแก้ได้ที่ `~/.sararif-cc/format.json`

## กฎความปลอดภัย (ห้ามละเมิด — แต่ละข้อมาจากงานที่เคยพังจริง)

- **ห้ามแก้ `draft_info.json` เอง** ไม่ว่ากรณีไหน — ใช้สคริปต์ในชุดนี้เท่านั้น (CapCut เก็บสำเนาไว้ 2 ที่ แก้เองไม่ครบ = งานหายเงียบๆ)
- **ห้ามรันตอน CapCut เปิดอยู่ / ห้าม bypass ตัวเช็ค** — สคริปต์ปิด CapCut ให้เองและไม่ยอมเขียนถ้าปิดไม่สำเร็จ นั่นคือพฤติกรรมที่ถูกต้อง อย่าพยายามหลบมัน
- **รันครั้งแรกกับโปรเจกต์ใหม่ทุกครั้ง ใช้ `--dry` ก่อน** แล้วให้เจ้าของเครื่องดูผลก่อนเขียนจริง
- **`order.ts` (เรียงคลิป) ต้องทำก่อนกด Auto captions** — เรียงทีหลังซับจะไม่ตรงภาพ ระบบเลยลบซับทิ้ง ต้องกดถอดใหม่
- พังแล้วอยากย้อน: ไฟล์สำรองอยู่ในโฟลเดอร์โปรเจกต์ `draft_info.json.PRE_CUT_BAK / .PRE_CC_BAK / .PRE_HOOK_BAK` — เอาไฟล์สำรองเขียนทับ `draft_info.json` แล้ว copy ทับทุกไฟล์ใน `Timelines/*/draft_info.json` ด้วย (สำคัญ)
- อย่าเพิ่มการพึ่งพาใหม่ (แพ็กเกจ/บริการ/คีย์) เพื่อแก้ปัญหาที่สคริปต์เดิมทำได้อยู่แล้ว

## แก้ปัญหาที่เจอบ่อย

| อาการ | ทาง |
|---|---|
| `ไม่พบโปรเจกต์ "X"` | ชื่อต้องตรงกับใน CapCut เป๊ะ — สคริปต์จะพิมพ์รายชื่อโปรเจกต์ที่มีให้เลือก |
| ซับไม่ขึ้นใน CapCut | ปิด CapCut ให้สนิท (Cmd+Q) แล้วเปิดใหม่ — มันแคช draft ไว้ |
| ซับซ้อนกัน 2 ชั้น | CapCut เปิดค้างตอนสคริปต์รัน — กู้จาก `.PRE_CC_BAK` แล้วรันใหม่โดยปิด CapCut ก่อน |
| ซับตัดชื่อร้าน/ชื่อคนขาดกลาง | เพิ่มคำนั้นใน `~/.sararif-cc/words.txt` (1 บรรทัด 1 คำ) แล้วรันใหม่ |
| ถอดเสียงได้คำผิดซ้ำๆ | เพิ่มใน `~/.sararif-cc/fix.txt` รูปแบบ `ผิด = ถูก` |
| มีข้อความหลายแทร็ก สคริปต์ถามให้เลือก | ดูรายการที่มันพิมพ์ แล้วรันซ้ำพร้อม `--track <เลข>` |
| เครื่องเป็น Windows | ยังไม่รองรับ — ดูหัวข้อ Windows ใน README (แก้ path + วิธีเช็คโปรเซส แล้วส่งกลับมาแบ่งคนอื่นได้) |

## ขอบเขต

เครื่องมือนี้ทำ: เรียงคลิป · ตัดช่วงเงียบ · ซับไทยแบ่งบรรทัดเป็น · คำเน้นสี · hook
มันไม่ทำ: import คลิป, กด Auto captions, เพลง, b-roll, ทรานซิชัน, export, โพสต์ — พวกนั้นเจ้าของเครื่องทำใน CapCut ตามปกติ อย่าสัญญาเกินนี้
